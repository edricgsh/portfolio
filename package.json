{
  "name": "edricgan-dev",
  "version": "1.0.0",
  "private": true,
  "description": "Edric Gan's portfolio, project archive, coaching profile, and writing.",
  "scripts": {
    "build": "node scripts/build.js",
    "check": "node scripts/check.js",
    "serve": "python3 -m http.server 4173 --directory dist"
  },
  "engines": {
    "node": ">=12"
  }
}
